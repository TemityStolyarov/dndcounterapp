import 'package:flutter/material.dart';

class TablePageScreen extends StatelessWidget {
  const TablePageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}